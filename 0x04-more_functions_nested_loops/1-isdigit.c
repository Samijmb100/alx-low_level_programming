#include "main.h"

/**
 * _isdigit - checks for digit
 *@c: inputed integer
 * Return: 1, 0
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
		return (1);
	else
		return (0);
	_putchar('\n');





}
